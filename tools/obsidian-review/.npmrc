; Config only — no credentials. eslint-plugin-obsidianmd's peer deps require
; legacy peer resolution; this keeps `npm install` and `npm ci` consistent in
; both local runs and the CI gate.
legacy-peer-deps=true
